import { DialogFormController } from './dialog-form-controller';

describe('DialogFormController', () => {
  it('should create an instance', () => {
    expect(new DialogFormController()).toBeTruthy();
  });
});
